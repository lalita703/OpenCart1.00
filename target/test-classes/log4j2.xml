<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="INFO">

    <!-- Define properties -->
    <Properties>
        <!-- Log directory -->
        <Property name="LOG_DIR">./logs</Property>
        <!-- File name prefix -->
        <Property name="LOG_FILE_NAME">automation</Property>
        <!-- Pattern for console/file output -->
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n</Property>
    </Properties>

    <!-- Appenders -->
    <Appenders>

        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>

        <!-- File Appender -->
        <File name="FileLogger" fileName="${LOG_DIR}/${LOG_FILE_NAME}.log" append="true">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </File>

        <!-- Rolling File Appender -->
        <RollingFile name="RollingFileLogger"
                     fileName="${LOG_DIR}/${LOG_FILE_NAME}.log"
                     filePattern="${LOG_DIR}/archive/${LOG_FILE_NAME}-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <!-- Roll over at 10 MB -->
                <SizeBasedTriggeringPolicy size="10 MB"/>
                <!-- Roll daily -->
                <TimeBasedTriggeringPolicy interval="1"/>
            </Policies>
            <!-- Keep 30 rolled files -->
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>

    </Appenders>

    <!-- Loggers -->
    <Loggers>

        <!-- Application package logger -->
        <Logger name="com.myapp" level="debug" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileLogger"/>
            <AppenderRef ref="RollingFileLogger"/>
        </Logger>

        <!-- Root logger -->
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileLogger"/>
            <AppenderRef ref="RollingFileLogger"/>
        </Root>
    </Loggers>
</Configuration>
